# آیکون‌های PWA هوشاگر

همهٔ آیکون‌ها از `public/logo.png` (1024×1024) با `npm run generate-icons` ساخته می‌شوند.

منبع طراحی: `assets/logo-source.png`

```bash
npm run logo:all
```

پس‌زمینهٔ maskable و apple-touch: `#10131A` (Smart Soft Dark).
